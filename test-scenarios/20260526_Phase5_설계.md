# 테스트 시나리오 — Phase 5: 설계 (Design) v2

- **생성일**: 2026-05-26
- **최종 수정일**: 2026-05-27
- **대상 Phase**: Phase 5
- **구현 기능**: 9-screen 설계 wizard — 백엔드(design.py 12개 엔드포인트, 4개 스킬, design_sessions + interview_sessions 테이블, 마이그레이션 006~007) + 프론트엔드(Frame/TopBar, AiQuestion, AiSuggestionList, StepTransition, DesignComplete, 3개 HelperPanel, 동적 SVG/파이프라인, 엔티티 추가/삭제)
- **전제조건**: 백엔드 서버 실행 중 (`uvicorn`), 프론트엔드 dev 서버 실행 중 (`npm run dev`), Supabase 연결 정상, Claude API 키 설정 완료, 로그인 완료, 인터뷰 완료 + "설계 진행" 선택된 프로젝트 존재 (status: `designing`)

---

## 시나리오 목록

| ID | 유형 | 제목 | 검증 | 우선순위 |
|----|------|------|------|---------|
| TC-001 | 정상 | Welcome 화면 진입 — 4단계 미리보기 + CTA 표시 | 👤 수동 | High |
| TC-002 | 정상 | Welcome "설계 시작" → Frame + 3컬럼 STEP 01 진입 | 👤 수동 | High |
| TC-003 | 정상 | 요구사항 — AiQuestion + 빈 상태 + AI 추천(인터뷰 기반) + 직접 입력 | 👤 수동 | High |
| TC-004 | 정상 | 요구사항 AI 생성 → 우선순위별 카드 목록 표시 | 👤 수동 | High |
| TC-005 | 정상 | STEP 01 완료 → StepTransition 전환 화면 → STEP 02 진입 | 👤 수동 | High |
| TC-006 | 정상 | 아키텍처 — 동적 템플릿 로딩 + 선택 | 👤 수동 | High |
| TC-007 | 정상 | 아키텍처 AI 생성 → 동적 SVG 다이어그램 + 부품 설명 카드 | 👤 수동 | High |
| TC-008 | 정상 | 데이터 모델 AI 생성 → 3컬럼 엔티티 그리드 + 관계 + 정합성 규칙 | 👤 수동 | High |
| TC-009 | 정상 | 데이터 모델 — 엔티티 추가 / 필드 추가 / 삭제 | 👤 수동 | Medium |
| TC-010 | 정상 | AI 워크플로우 자동 생성 → INPUT→AI→OUTPUT 파이프라인 + 폴백 카드 | 👤 수동 | High |
| TC-011 | 정상 | AI 워크플로우 건너뛰기 (비AI 프로젝트) | 🤖 AI검증 | Medium |
| TC-012 | 정상 | DesignComplete 완료 화면 — 통계 + 4행 요약 + 내보내기 + Phase 3 CTA | 👤 수동 | High |
| TC-013 | 정상 | 스텝 다음 → StepTransition 경유 이동 + 왼쪽 레일 업데이트 | 👤 수동 | High |
| TC-014 | 정상 | 스텝 이전 → 이전 스텝 복귀 + 기존 데이터 유지 | 👤 수동 | Medium |
| TC-015 | 정상 | 스텝 건너뛰기 → 생성 없이 다음 스텝 이동 | 👤 수동 | Medium |
| TC-016 | 정상 | 헬퍼 패널 표시 — 아키텍처/데이터/AI 스텝에서 우측 300px 패널 | 🤖 AI검증 | Medium |
| TC-017 | 정상 | 프로젝트 목록에서 "설계 중" 프로젝트 클릭 → 설계 페이지 이동 | 👤 수동 | High |
| TC-018 | 정상 | 프로젝트 목록 메뉴 "설계 이어하기" → 설계 페이지 이동 | 👤 수동 | Medium |
| TC-019 | 정상 | 인터뷰 완료 → "설계 진행" 클릭 → 설계 페이지로 리다이렉트 | 👤 수동 | High |
| TC-020 | 엣지 | 설계 세션 없이 진입 → Welcome 화면 표시 | 🤖 AI검증 | High |
| TC-021 | 엣지 | 브라우저 새로고침 → 기존 세션 복원 (스텝 상태 + 생성 데이터) | 👤 수동 | High |
| TC-022 | 엣지 | 잘못된 프로젝트 ID → 에러 화면 + "프로젝트 목록으로" 버튼 | 👤 수동 | Medium |
| TC-023 | 엣지 | 인터뷰 미완료 프로젝트 → /projects 리다이렉트 | 🤖 AI검증 | Medium |
| TC-024 | 엣지 | Welcome "나중에 결정할게요" → /projects 이동 | 🤖 AI검증 | Low |
| TC-025 | 실패 | Claude API 오류 → 빨간 에러 배너 + 재시도 가능 | 👤 수동 | High |
| TC-026 | 실패 | 생성 더블클릭 → 중복 방지 (disabled 상태) | 🤖 AI검증 | Medium |
| TC-027 | 실패 | Claude 응답 JSON 잘림 → 자동 복구 파싱 | 🤖 AI검증 | High |
| TC-028 | 정상 | design_sessions + arch_templates DB 저장 확인 | 🤖 AI검증 | High |
| TC-029 | 정상 | 인터뷰 _insights DB 영속화 (마이그레이션 007) | 🤖 AI검증 | High |
| TC-030 | 정상 | 동적 아키텍처 템플릿 API (/design/architecture/templates) | 🤖 AI검증 | High |
| TC-031 | 엣지 | 파이프라인 순서: requirements → architecture → data-model → ai-workflow 순차 입력 | 🤖 AI검증 | High |
| TC-032 | 정상 | _get_interview_context 키 수정 (_insights, content) 검증 | 🤖 AI검증 | High |

---

### TC-001 [정상] 👤 수동 — Welcome 화면 진입

**사전조건**: 인터뷰 완료 + "설계 진행" 선택된 프로젝트 (status: `designing`), design_sessions에 레코드 없음
**실행 단계**:
1. `/projects/{projectId}/design` URL로 이동
2. 로딩 화면("설계 환경을 준비하고 있습니다...") 확인
3. Welcome 화면 표시 대기
**입력값**: 없음 (자동 로딩)
**예상 결과**:
- 로딩 중 "P" 로고가 pulse 애니메이션으로 표시
- 로딩 완료 후 Frame(TopBar 56px) + Welcome 화면 표시:
  - "PHASE 2 of 3 · 시작" 알약 배지
  - 제목: "이제 **설계 단계**를 시작할게요"
  - 부제: "네 가지 질문에 답하시면 됩니다"
  - 2×2 그리드: 4단계 미리보기 카드 (기능 정의/시스템 구조/데이터 구조/AI 흐름, 각 아이콘+질문+~5분)
  - "완료하면 얻는 것" 초록 info 박스
  - 메인 CTA: "네, 설계를 시작할게요" (accent + 그림자)
  - 고스트 CTA: "나중에 결정할게요"
**확인 방법**: Welcome 화면 렌더링 확인. Network 탭에서 `GET /api/projects` → 200, `GET /api/design/session/{projectId}` → 404 (세션 없음 → welcome 표시).

**결과**: ✅ Pass
**테스트 일시**: 2026-06-08
**발견 문제**: —
**조치 상태**: —

---

### TC-002 [정상] 👤 수동 — Welcome "설계 시작" → STEP 01 진입

**사전조건**: TC-001 Welcome 화면 표시
**실행 단계**:
1. "네, 설계를 시작할게요" 버튼 클릭
2. 3컬럼 설계 wizard UI 표시 확인
**입력값**: 버튼 클릭
**예상 결과**:
- Frame + TopBar 유지 + 3컬럼 레이아웃:
  - **왼쪽 레일 (264px)**: 프로젝트명, 유형 태그, 진행률 "0/4", 4개 스텝 카드 (기능 정의=active, 나머지=pending)
  - **메인 콘텐츠**: DesignStepHeader "설계 · STEP 01 › 기능 정의" + AiQuestion 말풍선 + 빈 상태 카드 + AiSuggestionList
  - **하단 푸터**: 스텝별 라벨 "다음 질문 →" + "건너뛰기" + "자동 저장됨"
- 우측 헬퍼 패널은 requirements에서는 표시 안 됨
**확인 방법**: 3컬럼 렌더링 확인. DesignStepHeader에 "이 도구가 할 수 있는 일을 정의해볼까요?" 제목 표시.

**결과**: ✅ Pass
**테스트 일시**: 2026-06-08
**발견 문제**: —
**조치 상태**: —

---

### TC-003 [정상] 👤 수동 — 요구사항 — AiQuestion + 빈 상태 + AI 추천 + 직접 입력

**사전조건**: TC-002 완료, STEP 01 화면, 생성 전 상태
**실행 단계**:
1. AiQuestion 말풍선 확인
2. 빈 상태 카드 확인 ("아직 추가된 기능이 없어요")
3. AiSuggestionList (인터뷰 기반 AI 추천) 확인
4. 추천 항목 하나 클릭 → localItems에 추가
5. 직접 입력란에 텍스트 입력 → Enter 또는 "+ 추가" 버튼 클릭
**입력값**: 추천 항목 클릭 + 직접 텍스트 입력
**예상 결과**:
- **AiQuestion**: accent 테두리 카드, "AI가 묻는 질문" mono 라벨, "꼭 할 수 있어야 하는 일" 강조 + 힌트 박스
- **빈 상태**: dashed 테두리 + features 아이콘 + "직접 입력하거나, 아래 AI 추천에서 골라보세요"
- **AiSuggestionList**: "AI 추천 — 클릭으로 추가" 헤더, 인터뷰 insights 기반 추천 항목 (최대 8개), 각 항목에 "+" 원형 버튼
- 클릭 시 빈 상태 → 로컬 항목 리스트로 전환 (Must 배지 + 텍스트 + ✕ 삭제)
- 직접 입력: "Enter로 추가" 힌트 + "+ 추가" Btn
- Network: `GET /api/interview/session/{projectId}` → insights 배열 반환
**확인 방법**: AiSuggestionList 항목 클릭 → localItems 추가 확인. 직접 입력 → 리스트 추가 확인.

**결과**: ✅ Pass
**테스트 일시**: 2026-06-08
**발견 문제**: —
**조치 상태**: —

---

### TC-004 [정상] 👤 수동 — 요구사항 AI 생성 → 우선순위별 카드 목록

**사전조건**: STEP 01 화면, 푸터 "다음 질문 →" 클릭 또는 별도 생성 트리거
**실행 단계**:
1. 생성 트리거 (DesignPage의 handleGenerate → `POST /api/design/requirements/generate`)
2. 로딩 상태 확인
3. 생성 완료 대기 (10~30초)
**입력값**: 없음
**예상 결과**:
- **로딩 중**: AiQuestion "AI가 인터뷰 내용을 분석해서 기능 목록을 만들고 있습니다..." + pulse 애니메이션 + "약 15초 소요됩니다"
- **생성 완료**: AiQuestion "N개의 기능을 정리했어요. 확인하고 수정해주세요."
- 요구사항 카드 목록:
  - 우선순위 배지 (Must=`text-red bg-red-soft`, Should=`text-accent bg-accent-soft`, Could=`text-text-muted bg-surface-alt`)
  - 요구사항 텍스트 + 카테고리 mono 배지
- 하단 추가 입력 영역 유지 (추가 기능 입력 가능)
**확인 방법**: `POST /api/design/requirements/generate` → 200. 카드 개수 = 응답 `requirements` 배열 길이.

**결과**: ✅ Pass
**테스트 일시**: 2026-06-08
**발견 문제**: —
**조치 상태**: —

---

### TC-005 [정상] 👤 수동 — StepTransition 전환 화면 (STEP 01 → STEP 02)

**사전조건**: 요구사항 생성 완료 상태
**실행 단계**:
1. 푸터의 "다음 질문 →" 클릭
2. StepTransition 전환 화면 확인
3. "시스템 구조 시작하기" 클릭 → STEP 02 진입
**입력값**: 버튼 클릭
**예상 결과**:
- **전환 화면** (전체 화면, maxWidth 680px, 중앙 정렬):
  - 초록 그라데이션 체크 아이콘 (56px) + "STEP 01 COMPLETE" mono 라벨
  - "기능 정의 완료!" 제목
  - 요약 카드: "핵심 기능 N개 정의 완료", "우선순위별 분류 (Must N개 · Should N개 · Could N개)", "인수 기준 N개 자동 생성"
  - 다음 미리보기: accentSoft 카드, "다음 · STEP 02" + "시스템 구조" + "어떤 부품으로 만들지 결정해요..."
  - 버튼: "이전 단계 다시 보기" (secondary) + "시스템 구조 시작하기" (primary)
- "시스템 구조 시작하기" 클릭 → STEP 02 진입
- "이전 단계 다시 보기" 클릭 → STEP 01로 복귀
**확인 방법**: 전환 화면 렌더링 확인. 요약 내용이 실제 세션 데이터와 일치.

**결과**: ✅ Pass
**테스트 일시**: 2026-06-08
**발견 문제**: —
**조치 상태**: —

---

### TC-006 [정상] 👤 수동 — 아키텍처 — 동적 템플릿 로딩 + 선택

**사전조건**: STEP 02 (시스템 구조) 진입
**실행 단계**:
1. STEP 02 진입 → AI 템플릿 자동 로딩 확인 (onLoadTemplates 자동 호출)
2. 로딩 상태 ("프로젝트에 맞는 조합을 분석하고 있어요...") 확인
3. 템플릿 카드 표시 대기
4. 원하는 템플릿 클릭 → 선택 상태 확인
**입력값**: 템플릿 카드 클릭
**예상 결과**:
- **Explainer**: "시스템 구조 = 아키텍처 (Architecture)" + 레고 비유 + 예시
- **로딩 중**: pulse 애니메이션 + "AI" 배지 + 분석 중 텍스트
- **로딩 완료**: 2열 그리드에 AI 생성 TemplateCard 2개 표시
  - 첫 번째: badge="추천" (accent 배지)
  - 각 카드: 제목 + 설명 (구체적 기술 스택 포함)
  - 선택 시 accent 테두리 강조
- 하단 "AI로 아키텍처 설계하기" 버튼 (선택된 템플릿 기반)
- Network: `POST /api/design/architecture/templates` → 200, `templates` 배열 반환
**확인 방법**: 템플릿이 하드코딩이 아닌 AI 동적 생성 확인. 프로젝트별 다른 템플릿 내용.

**결과**: ✅ Pass
**테스트 일시**: 2026-06-08
**발견 문제**: —
**조치 상태**: —

---

### TC-007 [정상] 👤 수동 — 아키텍처 AI 생성 → 동적 SVG 다이어그램 + 부품 설명

**사전조건**: TC-006 완료, 템플릿 선택 상태
**실행 단계**:
1. "AI로 아키텍처 설계하기" 버튼 클릭
2. 로딩 상태 확인 ("AI가 시스템 구조를 설계하고 있습니다...")
3. 생성 완료 대기 (15~30초)
**입력값**: 선택된 templateIndex
**예상 결과**:
- **생성 완료 후**:
  - "선택한 조합 미리보기" + 선택 템플릿 Tag
  - **커스텀 SVG 아키텍처 다이어그램** (Mermaid 아님):
    - 컴포넌트별 색상 코딩: 파란(frontend) / 노란(backend) / 초록(data) / 빨강(AI) / 회색(external)
    - 레이어별 세로 정렬 + 화살표 연결
    - 하단 색상 범례: "사용자가 보는 것" / "저장되는 것" / "AI가 처리하는 것"
  - "각 부품을 왜 골랐나요?" — 번호 매긴 컴포넌트 설명 카드:
    - 번호 (색상 코딩) + 이름 + 기술 mono 배지 + 설명 + 역할 배지
- Network: `POST /api/design/architecture/generate` (body에 `template_index` 포함) → 200
- 응답 `architecture.components[]` 기반 SVG 동적 생성
**확인 방법**: SVG 다이어그램이 `architecture.components[]` 개수만큼 박스 렌더링. 색상이 역할별 올바르게 적용.

**결과**: ✅ Pass
**테스트 일시**: 2026-06-08
**발견 문제**: —
**조치 상태**: —

---

### TC-008 [정상] 👤 수동 — 데이터 모델 AI 생성 → 3컬럼 엔티티 그리드 + 관계 + 정합성 규칙

**사전조건**: 아키텍처 생성 완료, STEP 03 (데이터 구조) 진입
**실행 단계**:
1. STEP 03 진입 → Explainer 확인 ("데이터 구조 = 데이터 모델")
2. "AI로 데이터 구조 설계하기" 클릭
3. 생성 완료 대기 (15~45초)
**입력값**: 없음
**예상 결과**:
- **생성 완료 후**:
  - "저장할 정보 그룹 (테이블)" — **3컬럼 엔티티 카드 그리드**:
    - 각 카드: 이모지 아이콘 + 테이블명 + "N개" 필드 수 배지 + ✕ 삭제 버튼
    - 필드 리스트: 이름 + 타입 mono 배지 + 제약 (필수=red, 자동=green, PK/FK=accent)
    - "+ 항목 추가" dashed 버튼 (카드 하단)
  - "그룹 간 연결 관계" — 관계 카드:
    - from → 화살표 SVG → to + 설명 + 카디널리티 배지 (1:N) + ✕ 삭제
  - "+ 새 정보 그룹 추가" 전체 너비 dashed 버튼
  - "정합성 규칙 (자동 검증)" — 체크/경고 항목:
    - 초록 체크: 유니크, 필수 항목, FK 참조 등 (확인됨)
    - 노란 경고: enum/카테고리 항목 (확인 필요)
  - 우측 DataHelperPanel 표시 (엑셀 비유 + 타입 설명)
**확인 방법**: `POST /api/design/data-model/generate` → 200. 엔티티 카드 수 = `data_model.entities.length`. 정합성 규칙이 실제 필드 제약에서 동적 도출.

**결과**: ✅ Pass
**테스트 일시**: 2026-06-08
**발견 문제**: —
**조치 상태**: —

---

### TC-009 [정상] 👤 수동 — 데이터 모델 — 엔티티 추가 / 필드 추가 / 삭제

**사전조건**: TC-008 데이터 모델 생성 완료 상태
**실행 단계**:
1. "+ 새 정보 그룹 추가" 클릭 → 이름 입력 → Enter
2. 추가된 엔티티 카드 확인 (기본 필드: id/uuid/PK)
3. 엔티티 카드 하단 "+ 항목 추가" 클릭 → 필드명 + 타입 선택 → "추가"
4. 엔티티 카드 ✕ 클릭 → 엔티티 삭제
5. 관계 카드 ✕ 클릭 → 관계 삭제
**입력값**: 그룹명 "분석결과", 필드명 "score", 타입 "integer"
**예상 결과**:
- **엔티티 추가**: "분석결과" 카드 등장, 기본 필드 id(uuid, PK) 포함
- **필드 추가**: 기존 엔티티에 "score(integer)" 행 추가
- **엔티티 삭제**: 카드 즉시 사라짐 (확인 팝업 없음)
- **관계 삭제**: 관계 행 즉시 사라짐
- 모든 변경이 session state에 즉시 반영 (onUpdateSession)
**확인 방법**: 각 CRUD 동작 후 카드/필드 수 변화 육안 확인.

**결과**: ✅ Pass
**테스트 일시**: 2026-06-08
**발견 문제**: —
**조치 상태**: —

---

### TC-010 [정상] 👤 수동 — AI 워크플로우 자동 생성 → 파이프라인 + 폴백 카드

**사전조건**: 데이터 모델 완료, 프로젝트 유형에 "AI", "ML", "추천", "예측" 등 포함
**실행 단계**:
1. STEP 04 (AI 흐름) 진입 → 자동 생성 트리거 (useEffect)
2. 로딩 상태 확인 ("AI가 워크플로우를 자동으로 설계하고 있습니다...")
3. 완료 대기 (20~40초)
**입력값**: 없음 (자동 트리거)
**예상 결과**:
- **Explainer**: "AI 흐름 = AI Workflow" + I/O 예시
- **"AI의 입출력 흐름"** — 5컬럼 시각적 파이프라인:
  - INPUT 카드 (accentSoft, "📥 입력") → 화살표 → AI 카드 (그라데이션, 모델명+태스크 배지) → 화살표 → OUTPUT 카드 (greenSoft, "📤 출력")
  - INPUT/OUTPUT 리스트가 `session.ai_workflow` 마크다운에서 동적 파싱
- **"실패하면 어떻게 할까요? (폴백 전략)"** — 폴백 룰 카드:
  - 아이콘 (⏱/✕/💰 등) + 조건 + 대응 + "정의됨" 초록 배지
  - 운영 고려사항 섹션에서 동적 파싱
- 우측 AiHelperPanel 표시 (3가지 질문 가이드 + 폴백 팁)
**확인 방법**: `POST /api/design/ai-workflow/generate` → 200. 파이프라인 INPUT/OUTPUT 항목이 실제 마크다운 내용에서 추출. 빈 리스트일 경우 기본값 표시.

**결과**: ✅ Pass
**테스트 일시**: 2026-06-08
**발견 문제**: —
**조치 상태**: —

---

### TC-011 [정상] 🤖 AI검증 — AI 워크플로우 건너뛰기 (비AI 프로젝트)

**사전조건**: 프로젝트 유형이 AI/ML이 아닌 프로젝트 (예: "웹 쇼핑몰", "CMS")
**실행 단계**:
1. STEP 04 (AI 흐름) 진입
2. 비AI 프로젝트 안내 화면 확인
**입력값**: 없음
**예상 결과**:
- 자동 생성 트리거되지 않음
- 🚀 이모지 + "AI 워크플로우가 필요하지 않습니다" 제목
- "이 프로젝트는 AI/ML 유형이 아니므로 이 단계를 건너뜁니다." 안내
**확인 방법**:
- `AiWorkflowStep.tsx` — `isAiProject` = `/ai|ml|머신러닝|딥러닝|추천|예측|분류|생성/i.test(projectType)` ✅
- `useEffect` — `isAiProject && !aiWorkflow && !generating` 조건 false → `onGenerate()` 미호출 ✅
- `!isAiProject` 분기 → 건너뛰기 UI 렌더링 ✅

**결과**: ✅ Pass
**테스트 일시**: 2026-05-27
**발견 문제**: 없음
**조치 상태**: —

---

### TC-012 [정상] 👤 수동 — DesignComplete 완료 화면

**사전조건**: STEP 04 완료 후 "설계 완료" 클릭
**실행 단계**:
1. 마지막 스텝에서 "설계 완료" 클릭
2. 완료 화면 표시 확인
**입력값**: 없음
**예상 결과**:
- Frame 유지 + 전체 화면 완료 콘텐츠 (maxWidth 760px):
  - 🎉✨🎯🚀 bounce 애니메이션
  - "PHASE 2 COMPLETE" 배지 + "설계 단계 완료!" 제목
  - **통계 카드** (accentSoft):
    - "100%" + "N / 4 단계" + "설계 문서 v1.0 생성 완료" + 정의 항목 수
  - **4행 요약 테이블**: 각 스텝 아이콘 + 이름 + 동적 stats + "완료" 배지
    - 기능 정의: "기능 N개 · Must N개"
    - 시스템 구조: "부품 N개 · 기술 N개"
    - 데이터 구조: "그룹 N개 · 항목 N개"
    - AI 흐름: "워크플로우 정의 완료" 또는 "건너뜀"
  - **3개 내보내기 버튼**: Markdown / PDF / 공유 링크
  - **Phase 3 CTA**: 그라데이션 카드 "구현 단계로 넘어갈까요?" + "시작하기" 버튼
- "시작하기" 클릭 → `/projects`로 이동
**확인 방법**: 통계가 실제 세션 데이터와 일치하는지 확인.

**결과**: ✅ Pass
**테스트 일시**: 2026-06-08
**발견 문제**: —
**조치 상태**: —

---

### TC-013 [정상] 👤 수동 — 스텝 다음 → StepTransition 경유 이동

**사전조건**: STEP 01에서 요구사항 생성 완료
**실행 단계**:
1. 푸터 "다음 질문 →" 클릭
2. StepTransition 전환 화면 확인
3. "시스템 구조 시작하기" 클릭 → STEP 02 진입
4. 왼쪽 레일 상태 변화 확인
**입력값**: 없음
**예상 결과**:
- **다음 클릭 → StepTransition** (welcome→step이 아닌 step→transition 전환)
- 전환 화면에서 현재 스텝 요약 + 다음 스텝 미리보기 표시
- "시작하기" → STEP 02 진입
- 왼쪽 레일: "01 기능 정의" = done (체크), "02 시스템 구조" = active
- 푸터 라벨 변경: "다음 질문 →" → "데이터 구조로 →"
- 빵부스러기: "설계 · STEP 02 › 시스템 구조"
**확인 방법**: 전환 화면 → 다음 스텝 순서 확인. 왼쪽 레일 상태 동기화 확인.

**결과**: ✅ Pass
**테스트 일시**: 2026-06-08
**발견 문제**: —
**조치 상태**: —

---

### TC-014 [정상] 👤 수동 — 스텝 이전 → 이전 스텝 복귀

**사전조건**: STEP 02 이상에 위치
**실행 단계**:
1. 푸터의 "← 이전" 버튼 클릭
2. 이전 스텝 화면 표시 확인
**입력값**: 없음
**예상 결과**:
- 메인 콘텐츠가 이전 스텝으로 변경 (transition 화면 없이 직접 이동)
- 이전에 생성한 결과 데이터가 유지되어 표시
- STEP 01에서는 "← 이전" 버튼 미표시 (`canBack={!isFirstStep}`)
**확인 방법**: 이동 후 기존 생성 데이터 유지 확인.

**결과**: ✅ Pass
**테스트 일시**: 2026-06-08
**발견 문제**: —
**조치 상태**: —

---

### TC-015 [정상] 👤 수동 — 스텝 건너뛰기

**사전조건**: 아무 스텝, 생성 전 상태
**실행 단계**:
1. "생성하기" 버튼 누르지 않고 바로 "건너뛰기" 클릭
2. StepTransition 표시 → 다음 스텝 이동
**입력값**: 없음
**예상 결과**:
- StepTransition 화면에서 요약: "이 단계의 데이터를 불러오는 중입니다." (데이터 없음)
- 다음 스텝 이동 가능
- 왼쪽 레일: 건너뛴 스텝이 done 상태
- 이후 스텝 생성 시 이전 데이터가 null이어도 에러 없이 동작
**확인 방법**: 건너뛴 후 다음 스텝 생성이 에러 없이 동작하는지 확인.

**결과**: ✅ Pass
**테스트 일시**: 2026-06-08
**발견 문제**: —
**조치 상태**: —

---

### TC-016 [정상] 🤖 AI검증 — 헬퍼 패널 표시 (3개)

**사전조건**: 각 스텝 화면
**실행 단계**:
1. requirements 스텝 → 헬퍼 패널 없음 확인
2. architecture 스텝 → ArchHelperPanel 확인
3. data-model 스텝 → DataHelperPanel 확인
4. ai-workflow 스텝 → AiHelperPanel 확인
**입력값**: 없음
**예상 결과**:
- requirements: 헬퍼 패널 없음 (2컬럼 레이아웃)
- architecture: 우측 300px ArchHelperPanel (가이드 + 용어사전)
- data-model: 우측 300px DataHelperPanel (엑셀 비유 + 타입 설명)
- ai-workflow: 우측 300px AiHelperPanel (3가지 질문 가이드 + 폴백 팁)
**확인 방법**:
- `DesignPage.tsx` — `helperPanels` 객체: `{ architecture: <ArchHelperPanel />, 'data-model': <DataHelperPanel />, 'ai-workflow': <AiHelperPanel /> }` ✅
- `requirements` 키 없음 → `helperPanel = undefined` → 패널 미렌더링 ✅
- 패널 렌더 조건: `{helperPanel && <div className="w-[300px] ...">}` ✅

**결과**: ✅ Pass
**테스트 일시**: 2026-05-27
**발견 문제**: 없음
**조치 상태**: —

---

### TC-017 [정상] 👤 수동 — 프로젝트 목록 → 설계 페이지 이동

**사전조건**: status가 `designing`인 프로젝트가 목록에 존재
**실행 단계**:
1. `/projects` 페이지에서 "설계 중" 배지가 표시된 프로젝트 이름 클릭
**입력값**: 없음
**예상 결과**:
- `/projects/{projectId}/design` 페이지로 이동
- 기존 세션이 있으면 해당 스텝으로 바로 진입 (welcome 건너뜀)
- 기존 세션이 없으면 welcome 화면 표시
**확인 방법**: URL 확인 + 화면 상태 확인.

**결과**: ✅ Pass
**테스트 일시**: 2026-06-08
**발견 문제**: —
**조치 상태**: —

---

### TC-018 [정상] 👤 수동 — 프로젝트 목록 "설계 이어하기" 메뉴

**사전조건**: status가 `designing`인 프로젝트 존재
**실행 단계**:
1. ⋮ 메뉴 클릭 → "설계 이어하기" 항목 표시 확인
2. 클릭 → 설계 페이지 이동
**입력값**: 없음
**예상 결과**:
- "설계 이어하기" 메뉴가 accent 색상으로 표시
- 클릭 시 `/projects/{projectId}/design` 이동
**확인 방법**: 메뉴 렌더링 + 네비게이션 확인.

**결과**: ✅ Pass
**테스트 일시**: 2026-06-08
**발견 문제**: —
**조치 상태**: —

---

### TC-019 [정상] 👤 수동 — 인터뷰 완료 → 설계 진행 → 설계 페이지

**사전조건**: 인터뷰 완료 + 설계 결정 화면 표시
**실행 단계**:
1. "설계 진행" 카드 클릭
2. 이동 대상 확인
**입력값**: 없음
**예상 결과**:
- `POST /api/projects/{id}/design-decision` → status: `designing`
- `/projects/{projectId}/design` 페이지로 리다이렉트
- credits_used +1 차감
**확인 방법**: Network 탭 design-decision → 200, URL이 `/design`.

**결과**: ✅ Pass
**테스트 일시**: 2026-06-08
**발견 문제**: —
**조치 상태**: —

---

### TC-020 [엣지] 🤖 AI검증 — 설계 세션 없이 진입 → Welcome 화면

**사전조건**: `designing` 상태, design_sessions에 레코드 없음
**실행 단계**:
1. `/projects/{projectId}/design` 진입
**입력값**: 없음
**예상 결과**:
- `GET /api/design/session/{projectId}` → 404
- Welcome 화면 표시 (에러 아님)
**확인 방법**:
- `DesignPage.tsx` — `init()` 내 `try { apiFetch('/design/session/...') }` → `catch` 블록에서 `setScreen({ kind: 'welcome' })` ✅
- 세션 없으면 welcome, 있으면 해당 스텝 직접 진입 ✅

**결과**: ✅ Pass
**테스트 일시**: 2026-05-27
**발견 문제**: 없음
**조치 상태**: —

---

### TC-021 [엣지] 👤 수동 — 브라우저 새로고침 → 기존 세션 복원

**사전조건**: 요구사항 + 아키텍처까지 생성 완료한 상태
**실행 단계**:
1. F5 (새로고침)
2. 페이지 로딩 후 기존 세션 복원 확인
**입력값**: 없음
**예상 결과**:
- `GET /api/design/session/{projectId}`로 기존 세션 조회
- `current_step`에 해당하는 스텝 직접 진입 (welcome 건너뜀)
- 완료된 스텝에 체크 표시, 생성 결과 유지
- 아키텍처 SVG 다이어그램 재렌더링
**확인 방법**: 새로고침 전후 화면 동일 여부.

**결과**: ✅ Pass
**테스트 일시**: 2026-06-08
**발견 문제**: —
**조치 상태**: —

---

### TC-022 [엣지] 👤 수동 — 잘못된 프로젝트 ID → 에러 화면

**사전조건**: 없음
**실행 단계**:
1. `/projects/invalid-uuid/design` URL 접근
**입력값**: 없음
**예상 결과**:
- "프로젝트를 찾을 수 없습니다" 에러 메시지 + "프로젝트 목록으로" 버튼
- 클릭 시 `/projects` 이동
**확인 방법**: 에러 화면 렌더링 확인.

**결과**: ✅ Pass
**테스트 일시**: 2026-06-08
**발견 문제**: —
**조치 상태**: —

---

### TC-023 [엣지] 🤖 AI검증 — 인터뷰 미완료 → /projects 리다이렉트

**사전조건**: status가 `in_progress`인 프로젝트
**실행 단계**:
1. `/projects/{id}/design` URL 직접 접근
**입력값**: 없음
**예상 결과**:
- `/projects`로 즉시 리다이렉트
**확인 방법**:
- `DesignPage.tsx` — `init()`: `if (proj.status !== 'designing') { navigate('/projects'); return }` ✅
- `in_progress`, `evaluating`, `completed` 등 모두 리다이렉트 ✅

**결과**: ✅ Pass
**테스트 일시**: 2026-05-27
**발견 문제**: 없음
**조치 상태**: —

---

### TC-024 [엣지] 🤖 AI검증 — Welcome "나중에 결정할게요" → /projects 이동

**사전조건**: Welcome 화면 표시
**실행 단계**:
1. "나중에 결정할게요" 고스트 버튼 클릭
**입력값**: 없음
**예상 결과**:
- `/projects`로 이동
**확인 방법**:
- `DesignPage.tsx` — Welcome 렌더: `<DesignWelcome onDefer={() => navigate('/projects')} />` ✅
- `DesignWelcome.tsx` — "나중에 결정할게요" 버튼 `onClick={onDefer}` ✅

**결과**: ✅ Pass
**테스트 일시**: 2026-05-27
**발견 문제**: 없음
**조치 상태**: —

---

### TC-025 [실패] 👤 수동 — Claude API 오류 → 에러 배너

**사전조건**: Claude API 키 잘못 설정 또는 서비스 장애
**실행 단계**:
1. 아무 스텝에서 "AI로 ... 생성하기" 버튼 클릭
2. API 오류 발생 대기
**입력값**: 없음
**예상 결과**:
- 상단 빨간 에러 배너: 에러 메시지 + "닫기" 버튼 (bg-red/10 + text-red)
- "닫기" 클릭 시 에러 배너 사라짐
- 버튼 다시 활성화 (재시도 가능)
- 기존 데이터 유실 없음
**확인 방법**: `DesignPage.tsx` — `handleGenerate` catch 블록 `setError()`, finally 블록 `setGenerating(false)` 확인.

**결과**: ✅ Pass
**테스트 일시**: 2026-06-08
**발견 문제**: —
**조치 상태**: —

---

### TC-026 [실패] 🤖 AI검증 — 생성 더블클릭 → 중복 방지

**사전조건**: 아무 스텝, 생성 전 상태
**실행 단계**:
1. 생성 버튼 빠르게 2번 클릭
**입력값**: 없음
**예상 결과**:
- 첫 클릭만 처리, 두 번째 무시
**확인 방법**:
- `DesignPage.tsx` — `handleGenerate`: `if (!projectId || generating) return` 가드 ✅
- `RequirementsStep.tsx` — 생성 중: 로딩 UI 렌더링 (버튼 미표시) ✅
- `ArchitectureStep.tsx` — `disabled={loadingTemplates || !templates}` ✅
- `DataModelStep.tsx` — 생성 중: 로딩 UI 렌더링 ✅

**결과**: ✅ Pass
**테스트 일시**: 2026-05-27
**발견 문제**: 없음
**조치 상태**: —

---

### TC-027 [실패] 🤖 AI검증 — Claude 응답 JSON 잘림 → 자동 복구

**사전조건**: Claude API가 max_tokens 한도 도달
**실행 단계**:
1. 복잡한 프로젝트에서 데이터 모델 생성
**입력값**: 없음
**예상 결과**:
- `_repair_truncated_json()` 작동 → 잘린 JSON 자동 닫기
- 일부 누락 가능하나 에러 없이 200 반환
**확인 방법**:
- `design.py:_parse_json_response` — `json.loads()` 실패 시 `_repair_truncated_json()` 호출 ✅
- `_repair_truncated_json` — 역방향 탐색 + `]`/`}` 자동 닫기 ✅

**결과**: ✅ Pass
**테스트 일시**: 2026-05-27
**발견 문제**: 없음
**조치 상태**: —

---

### TC-028 [정상] 🤖 AI검증 — design_sessions + arch_templates DB 저장

**사전조건**: 생성 API 호출 완료
**실행 단계**:
1. design_sessions 테이블 레코드 확인
2. arch_templates 컬럼 저장 확인
**입력값**: 없음
**예상 결과**:
- `design_sessions` 테이블에 레코드 존재
- `requirements`, `architecture`, `data_model`, `ai_workflow` JSONB 정상 저장
- `arch_templates` JSONB 컬럼 정상 저장 (마이그레이션 007)
**확인 방법**:
- `design.py:_get_or_create_session` — `project_id` + `status=in_progress`로 조회/생성 ✅
- `generate_arch_templates` — `sb.table("design_sessions").update({"arch_templates": ...})` ✅
- `_session_to_out` — `arch_templates=session.get("arch_templates")` 응답에 포함 ✅
- `007_insights_and_arch_templates.sql` — `ADD COLUMN IF NOT EXISTS arch_templates JSONB` ✅

**결과**: ✅ Pass
**테스트 일시**: 2026-05-27
**발견 문제**: 없음
**조치 상태**: —

---

### TC-029 [정상] 🤖 AI검증 — 인터뷰 _insights DB 영속화 (마이그레이션 007)

**사전조건**: 인터뷰 진행 중 또는 완료
**실행 단계**:
1. 인터뷰 시작(start) 시 insights 저장 확인
2. 답변 제출(answer) 시 insights 업데이트 확인
3. 이어하기(resume) 시 insights 로드 확인
4. 세션 조회 시 insights 반환 확인
**입력값**: 없음
**예상 결과**:
- `_insights` JSONB 컬럼에 인터뷰 인사이트 누적 저장
- start/answer/resume 모든 플로우에서 정합성 유지
**확인 방법**:
- `interview.py:start_interview` — `session["_insights"] = insights` + `_save_session(session["id"], {"_insights": insights})` ✅
- `interview.py:submit_answer` — updates에 `"_insights": all_insights` 포함 ✅
- `interview.py:resume_interview` — `session["_insights"] = session.get("_insights") or []` (기존 값 보존) ✅
- `interview.py:get_session_by_project` — `.select("..., _insights")` 조회 + `insights` 변환 반환 ✅
- `007_insights_and_arch_templates.sql` — `ADD COLUMN IF NOT EXISTS _insights JSONB DEFAULT '[]'::jsonb` ✅

**결과**: ✅ Pass
**테스트 일시**: 2026-05-27
**발견 문제**: 없음
**조치 상태**: —

---

### TC-030 [정상] 🤖 AI검증 — 동적 아키텍처 템플릿 API

**사전조건**: 설계 세션 존재, 아키텍처 생성 전
**실행 단계**:
1. `POST /api/design/architecture/templates` 호출
2. AI가 프로젝트 유형에 맞는 템플릿 2개 생성
**입력값**: `{ "project_id": "..." }`
**예상 결과**:
- 200 응답 + `templates` 배열 (2개)
- 각 템플릿: `title`, `badge` ("추천" 또는 ""), `desc` (구체적 기술 스택)
- 이미 생성된 경우 캐시 반환 (재생성 안 함)
**확인 방법**:
- `design.py:generate_arch_templates` — 프로젝트 정보 + 인터뷰 컨텍스트 + 요구사항을 prompt에 전달 ✅
- 캐시: `if session.get("arch_templates"): return {"templates": session["arch_templates"]}` ✅
- 생성 후 DB 저장: `sb.table("design_sessions").update({"arch_templates": templates})` ✅
- 프론트: `DesignPage.tsx:handleLoadTemplates` → `setSession(prev => ({...prev, arch_templates: res.templates}))` ✅

**결과**: ✅ Pass
**테스트 일시**: 2026-05-27
**발견 문제**: 없음
**조치 상태**: —

---

### TC-031 [엣지] 🤖 AI검증 — 파이프라인 순서: 순차 입력 확인

**사전조건**: 설계 세션 존재
**실행 단계**:
1. 각 generate 함수가 이전 단계 데이터를 입력으로 사용하는지 확인
**입력값**: 없음
**예상 결과**:
- 각 단계가 이전 단계 결과를 컨텍스트로 전달
**확인 방법**:
- `generate_architecture` — `session.get("requirements")` → `requirements_text` + `template_text`(선택 템플릿) → `user_message` ✅
- `generate_data_model` — `session.get("requirements")` + `session.get("architecture")` → `prev_context` ✅
- `generate_ai_workflow` — `session.get("requirements")` + `session.get("architecture")` + `session.get("data_model")` → `prev_context` ✅

**결과**: ✅ Pass
**테스트 일시**: 2026-05-27
**발견 문제**: 없음
**조치 상태**: —

---

### TC-032 [정상] 🤖 AI검증 — _get_interview_context 키 수정 검증

**사전조건**: 인터뷰 완료된 세션 존재
**실행 단계**:
1. 설계 생성 시 인터뷰 컨텍스트 올바르게 로드되는지 확인
**입력값**: 없음
**예상 결과**:
- `_insights` 키로 인사이트 조회 (기존 `insights` 버그 수정)
- `content` 키로 메시지 텍스트 조회 (기존 `text` 버그 수정)
**확인 방법**:
- `design.py:_get_interview_context` — `insights = s.get("_insights", [])` (수정 전: `s.get("insights", [])`) ✅
- `text = msg.get("content", "")` (수정 전: `msg.get("text", "")`) ✅
- 이 수정 없이는 인터뷰 데이터가 빈 문자열로 전달되어 설계 품질 저하 ✅

**결과**: ✅ Pass
**테스트 일시**: 2026-05-27
**발견 문제**: 없음
**조치 상태**: —

---

## 테스트 요약

| 구분 | 개수 |
|------|------|
| 전체 | 32건 |
| 정상 | 21건 |
| 엣지 | 7건 |
| 실패 | 4건 |
| 👤 수동 | 19건 |
| 🤖 AI검증 | 13건 |

**총 결과**: 32건 중 Pass 32건 / Fail 0건
**테스트 일시**: 2026-06-08 (AI검증 12건은 2026-05-27)
**최종 상태**: ✅ 전체 Pass

---

## 테스트 완료 체크리스트

- [x] Welcome 화면 + 4단계 미리보기 + CTA (TC-001~002)
- [x] 요구사항: AiQuestion + AI 추천 + 직접 입력 + AI 생성 (TC-003~004)
- [x] StepTransition 전환 화면 + 동적 요약 (TC-005, 013)
- [x] 아키텍처: 동적 템플릿 로딩 + SVG 다이어그램 (TC-006~007)
- [x] 데이터 모델: 3컬럼 엔티티 + 관계 + 정합성 규칙 + CRUD (TC-008~009)
- [x] AI 워크플로우: 파이프라인 시각화 + 폴백 카드 (TC-010)
- [x] DesignComplete: 통계 + 요약 + 내보내기 (TC-012)
- [x] 스텝 네비게이션 (다음/이전/건너뛰기) (TC-013~015)
- [x] 프로젝트 목록 ↔ 설계 라우팅 (TC-017~019)
- [x] 세션 복원 (새로고침 후 데이터 유지) (TC-021)
- [x] 에러 처리 + 중복 방지 (TC-025~026)
- [x] AI검증 항목 코드 수준 확인 (TC-011, 016, 020, 023~024, 026~032) — 13건 전체 Pass
