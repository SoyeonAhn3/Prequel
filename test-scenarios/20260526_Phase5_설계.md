# 테스트 시나리오 — Phase 5: 설계 (Design)

- **생성일**: 2026-05-26
- **최종 수정일**: 2026-05-26
- **대상 Phase**: Phase 5
- **구현 기능**: 설계 파이프라인 전체 — 백엔드(design.py 11개 엔드포인트, 4개 스킬 파일, design_sessions 테이블) + 프론트엔드(3컬럼 wizard UI, 4개 스텝 컴포넌트, 초보자 프리미티브)
- **전제조건**: 백엔드 서버 실행 중 (`uvicorn`), 프론트엔드 dev 서버 실행 중 (`npm run dev`), Supabase 연결 정상, Claude API 키 설정 완료, 로그인 완료, 인터뷰 완료 + "설계 진행" 선택된 프로젝트 존재 (status: `designing`)

---

## 시나리오 목록

| ID | 유형 | 제목 | 검증 | 우선순위 |
|----|------|------|------|---------|
| TC-001 | 정상 | 설계 페이지 진입 → 3컬럼 wizard UI 표시 | 👤 수동 | High |
| TC-002 | 정상 | 요구사항 생성 → AI 분석 + 카드 목록 표시 | 👤 수동 | High |
| TC-003 | 정상 | 아키텍처 생성 → 컴포넌트 목록 + 기술 스택 표시 | 👤 수동 | High |
| TC-004 | 정상 | 데이터 모델 생성 → 엔티티 테이블 + 관계 표시 | 👤 수동 | High |
| TC-005 | 정상 | AI 워크플로우 자동 생성 (AI/ML 프로젝트) | 👤 수동 | High |
| TC-006 | 정상 | AI 워크플로우 건너뛰기 (비AI 프로젝트) | 🤖 AI검증 ✅ | Medium |
| TC-007 | 정상 | 스텝 네비게이션: 다음 단계 → 스텝 전진 + 왼쪽 레일 업데이트 | 👤 수동 | High |
| TC-008 | 정상 | 스텝 네비게이션: 이전 → 이전 스텝 복귀 | 👤 수동 | Medium |
| TC-009 | 정상 | 스텝 네비게이션: 건너뛰기 → 다음 스텝 이동 (생성 없이) | 👤 수동 | Medium |
| TC-010 | 정상 | 마지막 스텝 "설계 완료" → /projects 이동 | 👤 수동 | High |
| TC-011 | 정상 | 프로젝트 목록에서 "설계 중" 프로젝트 클릭 → 설계 페이지 이동 | 👤 수동 | High |
| TC-012 | 정상 | 프로젝트 목록 메뉴 "설계 이어하기" → 설계 페이지 이동 | 👤 수동 | Medium |
| TC-013 | 정상 | 인터뷰 완료 → "설계 진행" 클릭 → 설계 페이지로 리다이렉트 | 👤 수동 | High |
| TC-014 | 정상 | Explainer 카드 "이게 뭐예요?" 표시 + 기술 용어 배지 | 🤖 AI검증 ✅ | Medium |
| TC-015 | 정상 | ExampleBox "좋은 예시" 카드 표시 | 🤖 AI검증 ✅ | Low |
| TC-016 | 정상 | 요구사항 우선순위 배지 색상 (Must=빨강, Should=accent, Could=회색) | 🤖 AI검증 ✅ | Medium |
| TC-017 | 엣지 | 설계 세션 없이 페이지 진입 → 빈 상태 + "생성하기" 버튼 | 👤 수동 | High |
| TC-018 | 엣지 | 인터뷰 미완료 프로젝트로 설계 페이지 접근 → /projects 리다이렉트 | 🤖 AI검증 ✅ | Medium |
| TC-019 | 엣지 | 브라우저 새로고침 → 기존 세션 복원 | 👤 수동 | High |
| TC-020 | 엣지 | 잘못된 프로젝트 ID로 /design 접근 → 에러 화면 | 👤 수동 | Medium |
| TC-021 | 엣지 | 요구사항 파이프라인 순서: requirements → architecture → data-model 순차 입력 확인 | 🤖 AI검증 ✅ | High |
| TC-022 | 실패 | Claude API 오류 중 생성 버튼 클릭 → 에러 배너 표시 | 👤 수동 | High |
| TC-023 | 실패 | 생성 버튼 더블클릭 → 중복 생성 방지 (disabled 상태) | 🤖 AI검증 ✅ | Medium |
| TC-024 | 실패 | Claude 응답 JSON 잘림(토큰 초과) → 자동 복구 파싱 | 🤖 AI검증 ✅ | High |
| TC-025 | 정상 | 설계 세션 DB 저장 확인 (design_sessions 테이블) | 🤖 AI검증 ✅ | High |

---

### TC-001 [정상] 👤 수동 — 설계 페이지 진입 → 3컬럼 wizard UI 표시

**사전조건**: 인터뷰 완료 + "설계 진행" 선택된 프로젝트 존재 (status: `designing`)
**실행 단계**:
1. `/projects/{projectId}/design` URL로 이동
2. 로딩 화면("설계 환경을 준비하고 있습니다...") 확인
3. 3컬럼 wizard UI가 완전히 로딩될 때까지 대기
**입력값**: 없음 (자동 로딩)
**예상 결과**:
- 로딩 중 "P" 로고가 pulse 애니메이션으로 표시
- 로딩 완료 후 3컬럼 레이아웃이 표시:
  - **왼쪽 레일 (264px)**: "설계 단계" + 초록 상태 점 + "Phase 2 of 3", 프로젝트명, 유형 태그, 전체 진행률 "0/4", 4개 설계 스텝 카드 (기능 정의=active, 나머지=pending), "도움말 보기" 버튼
  - **메인 콘텐츠**: "설계 · STEP 01 › 기능 정의" 빵부스러기, "어떤 기능이 필요한가요?" 제목, 부제, Explainer 카드, ExampleBox, "AI로 요구사항 생성하기" 버튼
  - **하단 푸터**: "건너뛰기" + "다음 단계" 버튼, "자동 저장됨" 표시
- 상단 헤더에 Prequel 로고 + 프로젝트명
**확인 방법**: 3컬럼 모두 렌더링 확인. Network 탭에서 `GET /api/projects` → 200 확인.

**결과**: 🔲
**테스트 일시**: —
**발견 문제**: —
**조치 상태**: —

---

### TC-002 [정상] 👤 수동 — 요구사항 생성 → AI 분석 + 카드 목록 표시

**사전조건**: TC-001 완료, STEP 01 (기능 정의) 화면 표시
**실행 단계**:
1. "AI로 요구사항 생성하기" 버튼 클릭
2. 로딩 상태("AI가 분석하고 있습니다...") 확인
3. 생성 완료 대기 (10~30초)
**입력값**: 없음 (버튼 클릭만)
**예상 결과**:
- **로딩 중**: 버튼이 disabled + Loader2 스피너 + "AI가 분석하고 있습니다..." 텍스트
- **생성 완료**: 요구사항 카드 목록 표시 (8~20개)
- 각 카드에:
  - 우선순위 배지 (Must=빨강, Should=accent, Could=회색)
  - 요구사항 텍스트 (한국어)
  - 카테고리 라벨
  - 인수 기준 (회색 배경 박스)
- 상단에 "총 N개 요구사항이 생성되었습니다" 카운트 표시
**확인 방법**: Network 탭에서 `POST /api/design/requirements/generate` → 200 확인. 응답 JSON에 `requirements` 배열 존재. 카드 개수와 응답 배열 길이 일치.

**결과**: 🔲
**테스트 일시**: —
**발견 문제**: —
**조치 상태**: —

---

### TC-003 [정상] 👤 수동 — 아키텍처 생성 → 컴포넌트 목록 + 기술 스택 표시

**사전조건**: 요구사항 생성 완료, STEP 02 (시스템 구조)로 이동
**실행 단계**:
1. "다음 단계" 클릭 → STEP 02 진입
2. Explainer 카드("시스템 아키텍처란?") 확인
3. "AI로 아키텍처 설계하기" 클릭
4. 생성 완료 대기
**입력값**: 없음
**예상 결과**:
- **생성 완료 후**:
  - Mermaid 다이어그램 영역 표시 (SYSTEM DIAGRAM 헤더)
  - 컴포넌트 카드 그리드 (2열, 3~8개)
  - 각 카드: 이름, 설명, 기술 스택 배지 (accent-soft), 역할 배지
  - 하단 TECH STACK 섹션: key-value 쌍으로 기술 스택 나열
**확인 방법**: `POST /api/design/architecture/generate` → 200. 응답에 `architecture.components`, `architecture.tech_stack`, `architecture.mermaid_code` 존재.

**결과**: 🔲
**테스트 일시**: —
**발견 문제**: —
**조치 상태**: —

---

### TC-004 [정상] 👤 수동 — 데이터 모델 생성 → 엔티티 테이블 + 관계 표시

**사전조건**: 아키텍처 생성 완료, STEP 03 (데이터 구조)으로 이동
**실행 단계**:
1. "다음 단계" 클릭 → STEP 03 진입
2. Explainer 카드("데이터 모델이란?") 확인
3. "AI로 데이터 구조 설계하기" 클릭
4. 생성 완료 대기 (15~45초, 가장 오래 걸림)
**입력값**: 없음
**예상 결과**:
- **생성 완료 후**:
  - Mermaid ERD 영역 표시 (ERD 헤더)
  - 엔티티 카드 목록 (3~7개 테이블)
  - 각 카드: 테이블명 (mono 폰트), 설명, 필드 테이블 (필드명/타입/설명/제약)
  - 하단 RELATIONSHIPS 섹션: 관계 설명 bullet list
- 필드 테이블에서 PK/FK/NOT NULL 등 제약 표시
**확인 방법**: `POST /api/design/data-model/generate` → 200. 응답에 `data_model.entities`, `data_model.relationships` 존재. JSON이 잘렸다면 자동 복구 파싱이 동작 (서버 로그에 에러 없음).

**결과**: 🔲
**테스트 일시**: —
**발견 문제**: —
**조치 상태**: —

---

### TC-005 [정상] 👤 수동 — AI 워크플로우 자동 생성 (AI/ML 프로젝트)

**사전조건**: 데이터 모델 생성 완료, 프로젝트 유형에 "AI", "ML", "추천", "예측" 등 키워드 포함
**실행 단계**:
1. "다음 단계" 클릭 → STEP 04 (AI 흐름) 진입
2. 자동 생성이 시작되는지 확인 (useEffect 트리거)
3. 로딩 상태("AI가 워크플로우를 자동으로 설계하고 있습니다...") 확인
4. 완료 대기
**입력값**: 없음 (자동 트리거)
**예상 결과**:
- STEP 04 진입 시 자동으로 `POST /api/design/ai-workflow/generate` 호출
- 로딩 중 Loader2 스피너 + "약 30초 소요됩니다" 안내
- 완료 후 마크다운 문서 렌더링:
  - "AI 활용 요약" 섹션
  - "모델 선택" 섹션
  - "데이터 파이프라인" 섹션
  - "추론 흐름" 섹션
  - "평가 지표" 섹션
  - "운영 고려사항" 섹션
- 세션 status가 `completed`로 변경
**확인 방법**: `POST /api/design/ai-workflow/generate` → 200. 응답에 `ai_workflow` (마크다운 텍스트), `status: "completed"`.

**결과**: 🔲
**테스트 일시**: —
**발견 문제**: —
**조치 상태**: —

---

### TC-006 [정상] 🤖 AI검증 — AI 워크플로우 건너뛰기 (비AI 프로젝트)

**사전조건**: 프로젝트 유형이 AI/ML이 아닌 프로젝트 (예: "웹 쇼핑몰", "CMS")
**실행 단계**:
1. STEP 04 (AI 흐름) 진입
2. 비AI 프로젝트 안내 화면 확인
**입력값**: 없음
**예상 결과**:
- 자동 생성이 트리거되지 않음
- "AI 워크플로우가 필요하지 않습니다" 메시지 표시
- 로켓 이모지 아이콘 + "이 프로젝트는 AI/ML 유형이 아니므로 이 단계를 건너뜁니다." 설명
- "다음 단계 버튼을 눌러 설계를 완료해주세요" 안내
**확인 방법**:
- `AiWorkflowStep.tsx` — `isAiProject` 정규식: `/ai|ml|머신러닝|딥러닝|추천|예측|분류|생성/i.test(projectType)` ✅
- `projectType`이 위 패턴에 매치하지 않으면 `isAiProject = false` → 건너뛰기 UI 렌더 ✅
- `useEffect` — `isAiProject && !aiWorkflow && !generating` 조건이 false이므로 `onGenerate()` 호출 안 됨 ✅

**결과**: ✅ Pass
**테스트 일시**: 2026-05-26
**발견 문제**: 없음
**조치 상태**: —

---

### TC-007 [정상] 👤 수동 — 스텝 네비게이션: 다음 단계 → 스텝 전진 + 왼쪽 레일 업데이트

**사전조건**: STEP 01에서 요구사항 생성 완료
**실행 단계**:
1. 푸터의 "다음 단계" 버튼 클릭
2. 왼쪽 레일 + 메인 콘텐츠 변화 확인
**입력값**: 없음
**예상 결과**:
- 메인 콘텐츠: STEP 02 (시스템 구조) 헤더 + Explainer + 생성 버튼으로 변경
- 왼쪽 레일: "01 기능 정의"에 초록 체크 아이콘 (done), "02 시스템 구조"가 accent 배경 + 진한 글씨 (active)
- 전체 진행률 "1/4"로 업데이트
- 빵부스러기: "설계 · STEP 02 › 시스템 구조"
**확인 방법**: 왼쪽 레일 스텝 상태 변화 육안 확인.

**결과**: 🔲
**테스트 일시**: —
**발견 문제**: —
**조치 상태**: —

---

### TC-008 [정상] 👤 수동 — 스텝 네비게이션: 이전 → 이전 스텝 복귀

**사전조건**: STEP 02 이상에 위치
**실행 단계**:
1. 푸터의 "← 이전" 버튼 클릭
2. 이전 스텝으로 돌아가는지 확인
**입력값**: 없음
**예상 결과**:
- 메인 콘텐츠가 이전 스텝 내용으로 변경
- 이전에 생성한 결과가 유지되어 표시
- STEP 01에서는 "← 이전" 버튼이 표시되지 않음
**확인 방법**: 스텝 이동 후 기존 생성 데이터 유지 확인.

**결과**: 🔲
**테스트 일시**: —
**발견 문제**: —
**조치 상태**: —

---

### TC-009 [정상] 👤 수동 — 스텝 네비게이션: 건너뛰기 → 다음 스텝 이동 (생성 없이)

**사전조건**: 아무 스텝에서 생성 전 상태
**실행 단계**:
1. "AI로 ... 생성하기" 버튼을 누르지 않고 바로 "건너뛰기" 클릭
2. 다음 스텝으로 이동하는지 확인
**입력값**: 없음
**예상 결과**:
- 생성 없이 다음 스텝으로 이동
- 왼쪽 레일에서 건너뛴 스텝이 done 상태로 변경
- 이전 스텝의 데이터가 null인 상태로 다음 스텝 생성에 영향 없음
**확인 방법**: 건너뛴 후 다음 스텝 생성이 에러 없이 동작하는지 확인.

**결과**: 🔲
**테스트 일시**: —
**발견 문제**: —
**조치 상태**: —

---

### TC-010 [정상] 👤 수동 — 마지막 스텝 "설계 완료" → /projects 이동

**사전조건**: STEP 04 (AI 흐름)에 위치
**실행 단계**:
1. STEP 04에서 "설계 완료" 버튼 클릭 (마지막 스텝이므로 primaryLabel = "설계 완료")
2. /projects 페이지로 이동하는지 확인
**입력값**: 없음
**예상 결과**:
- `/projects` 페이지로 리다이렉트
- 프로젝트 목록에서 해당 프로젝트가 표시됨
**확인 방법**: URL이 `/projects`로 변경 확인.

**결과**: 🔲
**테스트 일시**: —
**발견 문제**: —
**조치 상태**: —

---

### TC-011 [정상] 👤 수동 — 프로젝트 목록에서 "설계 중" 프로젝트 클릭 → 설계 페이지 이동

**사전조건**: status가 `designing`인 프로젝트가 프로젝트 목록에 존재
**실행 단계**:
1. `/projects` 페이지에서 "설계 중" 배지가 표시된 프로젝트 이름 클릭
**입력값**: 없음
**예상 결과**:
- `/projects/{projectId}/design` 페이지로 이동
- 상태 배지가 "설계 중" (accent 색상)으로 표시
- 프로젝트 이름 클릭 시 `/interview`가 아닌 `/design`으로 이동
**확인 방법**: URL 확인 + STATUS_MAP에 `designing: { label: '설계 중', variant: 'accent' }` 존재 확인.

**결과**: 🔲
**테스트 일시**: —
**발견 문제**: —
**조치 상태**: —

---

### TC-012 [정상] 👤 수동 — 프로젝트 목록 메뉴 "설계 이어하기" → 설계 페이지 이동

**사전조건**: status가 `designing`인 프로젝트 존재
**실행 단계**:
1. 프로젝트 행의 ⋮ 메뉴 클릭
2. "설계 이어하기" 메뉴 항목 표시 확인
3. 클릭 → 설계 페이지 이동 확인
**입력값**: 없음
**예상 결과**:
- ⋮ 메뉴에 "설계 이어하기" 항목이 accent 색상으로 표시
- 클릭 시 `/projects/{projectId}/design`으로 이동
- `in_progress` 프로젝트에서는 "이어하기"(인터뷰), `designing`에서는 "설계 이어하기" 표시
**확인 방법**: 메뉴 렌더링 + 네비게이션 확인.

**결과**: 🔲
**테스트 일시**: —
**발견 문제**: —
**조치 상태**: —

---

### TC-013 [정상] 👤 수동 — 인터뷰 완료 → "설계 진행" 클릭 → 설계 페이지로 리다이렉트

**사전조건**: 인터뷰 10 STEP + AI 제안까지 완료된 프로젝트, 설계 결정 화면 표시
**실행 단계**:
1. "설계 진행" 카드 클릭
2. 이동 대상 확인
**입력값**: 없음
**예상 결과**:
- `POST /api/projects/{id}/design-decision` (body: `{decision: "design"}`) → status: `designing`
- `/projects/{projectId}/design` 페이지로 리다이렉트 (기존: `/projects`로 이동했음)
- credits_used +1 차감
**확인 방법**: Network 탭 design-decision → 200, 이후 URL이 `/design`인지 확인.

**결과**: 🔲
**테스트 일시**: —
**발견 문제**: —
**조치 상태**: —

---

### TC-014 [정상] 🤖 AI검증 — Explainer 카드 "이게 뭐예요?" 표시 + 기술 용어 배지

**사전조건**: STEP 01 (기능 정의) 화면, 생성 전 상태
**실행 단계**:
1. Explainer 컴포넌트 렌더링 확인
**입력값**: 없음
**예상 결과**:
- "이게 뭐예요?" 헤더 + 물음표 아이콘
- 기술 용어 배지: "Functional Requirements" (mono 폰트, 반투명)
- 제목: "기능 요구사항이란?"
- 쉬운 한국어 설명 + 예시 문단
**확인 방법**:
- `RequirementsStep.tsx` — 생성 전 분기에서 `<Explainer title="기능 요구사항이란?" technical="Functional Requirements" ...>` 렌더링 ✅
- `Explainer.tsx` — `technical` prop이 있으면 mono 배지 표시 ✅
- `example` prop이 있으면 점선 구분 + "예시 ·" 프리픽스 표시 ✅

**결과**: ✅ Pass
**테스트 일시**: 2026-05-26
**발견 문제**: 없음
**조치 상태**: —

---

### TC-015 [정상] 🤖 AI검증 — ExampleBox "좋은 예시" 카드 표시

**사전조건**: STEP 01 (기능 정의) 화면, 생성 전 상태
**실행 단계**:
1. ExampleBox 컴포넌트 렌더링 확인
**입력값**: 없음
**예상 결과**:
- 초록 배경 카드, "좋은 예시" 라벨 (볼드)
- 내용: "Must(필수): 로그인, 검색 · Should(권장): 알림, 대시보드 · Could(선택): 다크모드"
**확인 방법**:
- `RequirementsStep.tsx` — `<ExampleBox>Must(필수): 로그인, 검색 ...</ExampleBox>` 렌더링 ✅
- `ExampleBox.tsx` — `bg-green-soft` 배경 + `font-bold` 라벨 ✅

**결과**: ✅ Pass
**테스트 일시**: 2026-05-26
**발견 문제**: 없음
**조치 상태**: —

---

### TC-016 [정상] 🤖 AI검증 — 요구사항 우선순위 배지 색상

**사전조건**: 요구사항 생성 완료 상태
**실행 단계**:
1. Must/Should/Could 배지 색상 확인
**입력값**: 없음
**예상 결과**:
- Must: 빨강 계열 (`--color-red`)
- Should: accent 계열 (`--color-accent`)
- Could: 회색 계열 (`--color-text-muted`)
**확인 방법**:
- `RequirementsStep.tsx` — `PRIORITY_LABELS` 객체:
  - `must: { label: 'Must', color: 'var(--color-red)' }` ✅
  - `should: { label: 'Should', color: 'var(--color-accent)' }` ✅
  - `could: { label: 'Could', color: 'var(--color-text-muted)' }` ✅
- `RequirementCard` — `color: priority.color` + `background: color-mix(... 10%)` 적용 ✅

**결과**: ✅ Pass
**테스트 일시**: 2026-05-26
**발견 문제**: 없음
**조치 상태**: —

---

### TC-017 [엣지] 👤 수동 — 설계 세션 없이 페이지 진입 → 빈 상태 + "생성하기" 버튼

**사전조건**: `designing` 상태이지만 design_sessions 테이블에 레코드가 없는 프로젝트
**실행 단계**:
1. `/projects/{projectId}/design` 페이지 진입
2. STEP 01 화면 확인
**입력값**: 없음
**예상 결과**:
- 에러 없이 STEP 01 (기능 정의) 화면 표시
- Explainer + ExampleBox + "AI로 요구사항 생성하기" 버튼 표시
- 세션은 첫 생성 시 자동 생성 (`_get_or_create_session`)
**확인 방법**: `GET /api/design/session/{projectId}` → 404 (세션 없음) 후에도 페이지가 정상 렌더링. 생성 클릭 시 세션 자동 생성.

**결과**: 🔲
**테스트 일시**: —
**발견 문제**: —
**조치 상태**: —

---

### TC-018 [엣지] 🤖 AI검증 — 인터뷰 미완료 프로젝트로 설계 페이지 접근 → /projects 리다이렉트

**사전조건**: status가 `in_progress`인 프로젝트 (아직 인터뷰 중)
**실행 단계**:
1. 해당 프로젝트 ID로 `/projects/{id}/design` URL 직접 접근
**입력값**: 없음
**예상 결과**:
- `/projects`로 즉시 리다이렉트
**확인 방법**:
- `DesignPage.tsx` — `init()` 함수에서 `proj.status !== 'designing'` 이면 `navigate('/projects')` 호출 ✅
- `in_progress`, `evaluating`, `completed` 모두 리다이렉트 대상 ✅

**결과**: ✅ Pass
**테스트 일시**: 2026-05-26
**발견 문제**: 없음
**조치 상태**: —

---

### TC-019 [엣지] 👤 수동 — 브라우저 새로고침 → 기존 세션 복원

**사전조건**: 요구사항 + 아키텍처까지 생성 완료한 상태
**실행 단계**:
1. F5 (새로고침)
2. 페이지 로딩 후 기존 세션 데이터 복원 확인
**입력값**: 없음
**예상 결과**:
- `GET /api/design/session/{projectId}`로 기존 세션 조회
- `current_step`, `requirements`, `architecture` 등 기존 데이터 복원
- 왼쪽 레일: 완료된 스텝에 체크 표시, 마지막 진행 스텝이 active
- 메인 콘텐츠: 해당 스텝의 생성 결과가 표시됨
**확인 방법**: 새로고침 전후 화면 동일 여부 확인. Network에서 session 응답에 기존 데이터 포함 확인.

**결과**: 🔲
**테스트 일시**: —
**발견 문제**: —
**조치 상태**: —

---

### TC-020 [엣지] 👤 수동 — 잘못된 프로젝트 ID로 /design 접근 → 에러 화면

**사전조건**: 없음
**실행 단계**:
1. `/projects/invalid-uuid/design` URL 접근
**입력값**: 없음
**예상 결과**:
- "프로젝트를 찾을 수 없습니다" 에러 메시지 표시
- "프로젝트 목록으로" 버튼 표시
- 클릭 시 `/projects`로 이동
**확인 방법**: 에러 화면 렌더링 확인.

**결과**: 🔲
**테스트 일시**: —
**발견 문제**: —
**조치 상태**: —

---

### TC-021 [엣지] 🤖 AI검증 — 요구사항 파이프라인 순서: 순차 입력 확인

**사전조건**: 설계 세션 존재
**실행 단계**:
1. 아키텍처 생성 시 요구사항이 입력 컨텍스트에 포함되는지 확인
2. 데이터 모델 생성 시 요구사항 + 아키텍처가 입력 컨텍스트에 포함되는지 확인
3. AI 워크플로우 생성 시 요구사항 + 아키텍처 + 데이터 모델이 입력 컨텍스트에 포함되는지 확인
**입력값**: 없음
**예상 결과**:
- 각 단계가 이전 단계 결과를 입력으로 사용
**확인 방법**:
- `design.py:generate_architecture` — `session.get("requirements")` → `requirements_text`로 변환 후 `user_message`에 포함 ✅
- `design.py:generate_data_model` — `session.get("requirements")` + `session.get("architecture")` → `prev_context`로 변환 ✅
- `design.py:generate_ai_workflow` — `session.get("requirements")` + `session.get("architecture")` + `session.get("data_model")` → `prev_context`로 변환 ✅

**결과**: ✅ Pass
**테스트 일시**: 2026-05-26
**발견 문제**: 없음
**조치 상태**: —

---

### TC-022 [실패] 👤 수동 — Claude API 오류 중 생성 버튼 클릭 → 에러 배너 표시

**사전조건**: Claude API 키가 잘못 설정되었거나, API 서비스 장애 상태
**실행 단계**:
1. "AI로 요구사항 생성하기" 버튼 클릭
2. API 오류 발생 대기
**입력값**: 없음
**예상 결과**:
- 상단에 빨간 에러 배너 표시 (에러 메시지 + "닫기" 버튼)
- "생성하기" 버튼이 다시 활성화 (재시도 가능)
- 기존 데이터 유실 없음
**확인 방법**: `DesignPage.tsx` — `handleGenerate` catch 블록에서 `setError()` 호출, `finally`에서 `setGenerating(false)` 확인.

**결과**: 🔲
**테스트 일시**: —
**발견 문제**: —
**조치 상태**: —

---

### TC-023 [실패] 🤖 AI검증 — 생성 버튼 더블클릭 → 중복 생성 방지

**사전조건**: 아무 스텝에서 생성 전 상태
**실행 단계**:
1. "AI로 ... 생성하기" 버튼을 빠르게 2번 클릭
**입력값**: 없음
**예상 결과**:
- 첫 클릭만 처리, 두 번째 클릭은 무시
- 중복 API 호출 없음
**확인 방법**:
- `DesignPage.tsx` — `handleGenerate`에서 `if (generating) return` 가드 ✅
- 각 스텝 컴포넌트 — 버튼에 `disabled={generating}` prop ✅
- `RequirementsStep.tsx:42` — `<button disabled={generating}>` ✅
- `ArchitectureStep.tsx:35` — `<button disabled={generating}>` ✅
- `DataModelStep.tsx:35` — `<button disabled={generating}>` ✅

**결과**: ✅ Pass
**테스트 일시**: 2026-05-26
**발견 문제**: 없음
**조치 상태**: —

---

### TC-024 [실패] 🤖 AI검증 — Claude 응답 JSON 잘림(토큰 초과) → 자동 복구 파싱

**사전조건**: Claude API가 max_tokens 한도에 도달하여 JSON이 잘린 응답 반환
**실행 단계**:
1. 복잡한 프로젝트에서 데이터 모델 생성 (엔티티가 많아 JSON이 길어지는 경우)
**입력값**: 없음
**예상 결과**:
- `_repair_truncated_json()` 함수가 잘린 JSON을 복구
- 일부 데이터가 누락될 수 있으나 에러 없이 결과 반환
- 서버 500 에러가 아닌 정상 200 응답
**확인 방법**:
- `design.py:_parse_json_response` — `json.loads()` 실패 시 `_repair_truncated_json()` 호출 ✅
- `_repair_truncated_json` — 끝에서부터 역방향 탐색, 열린 괄호 `]`/`}` 자동 닫기 ✅
- E2E 테스트 (curl): 데이터 모델 생성 시 실제 JSON 잘림 발생 → 7개 엔티티로 정상 복구된 이력 ✅

**결과**: ✅ Pass
**테스트 일시**: 2026-05-26
**발견 문제**: 없음
**조치 상태**: —

---

### TC-025 [정상] 🤖 AI검증 — 설계 세션 DB 저장 확인 (design_sessions 테이블)

**사전조건**: 요구사항 생성 API 호출 완료
**실행 단계**:
1. 생성 후 design_sessions 테이블에 레코드가 저장되는지 확인
**입력값**: 없음
**예상 결과**:
- `design_sessions` 테이블에 레코드 존재
- `project_id`, `current_step`, `requirements` (JSONB), `status` 필드 정상 저장
- 후속 생성 시 같은 세션 업데이트 (새 세션 생성 안 함)
**확인 방법**:
- `design.py:_get_or_create_session` — `design_sessions`에서 `project_id` + `status=in_progress`로 조회, 없으면 INSERT ✅
- `generate_requirements` — 생성 후 `sb.table("design_sessions").update({"requirements": ...}).eq("id", session["id"])` 호출 ✅
- `006_design_sessions.sql` — 테이블 스키마 정상, PK=uuid, project_id FK ✅

**결과**: ✅ Pass
**테스트 일시**: 2026-05-26
**발견 문제**: 없음
**조치 상태**: —

---

## 테스트 요약

| 구분 | 개수 |
|------|------|
| 전체 | 25건 |
| 정상 | 16건 |
| 엣지 | 5건 |
| 실패 | 4건 |
| 👤 수동 | 15건 |
| 🤖 AI검증 | 10건 |

---

## 테스트 완료 체크리스트

- [ ] 설계 페이지 3컬럼 UI 정상 렌더링 (TC-001)
- [ ] 4단계 생성 파이프라인 E2E (TC-002~005)
- [ ] 스텝 네비게이션 (이전/다음/건너뛰기) 정상 (TC-007~009)
- [ ] 프로젝트 목록 ↔ 설계 페이지 라우팅 정상 (TC-011~013)
- [ ] 에러 처리 + 중복 방지 동작 (TC-022~023)
- [ ] 세션 복원 (새로고침 후 데이터 유지) (TC-019)
- [ ] JSON 잘림 자동 복구 검증 (TC-024)
- [x] AI검증 항목 코드 수준 확인 (TC-006, 014~016, 018, 021, 023~025) — 10건 전체 Pass
