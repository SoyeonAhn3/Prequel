# 테스트 시나리오 — Phase 3 프로젝트 관리

- **생성일**: 2026-05-20
- **대상 Phase**: Phase 3
- **구현 기능**: 프로젝트 CRUD API, 무료 쿼터 검증, 내 프로젝트 페이지 (스탯카드/필터/검색/테이블), 새 프로젝트 생성 모달, 삭제 확인 모달
- **전제조건**: 백엔드(`uvicorn app.main:app --reload --port 8000`) + 프론트엔드(`npm run dev`) 실행 중, OAuth 로그인 완료, Supabase 마이그레이션 005 적용 완료

---

## 시나리오 목록

| ID | 유형 | 제목 | 검증 | 우선순위 |
|----|------|------|------|---------|
| TC-001 | 정상 | 한국어 프로젝트 생성 전체 플로우 | 👤 수동 | High |
| TC-002 | 정상 | 프로젝트 목록 조회 및 스탯카드 표시 | 👤 수동 | High |
| TC-003 | 정상 | 프로젝트 삭제 (soft-delete) | 👤 수동 | High |
| TC-004 | 정상 | 필터 탭 전환 (전체/진행 중/완료) | 👤 수동 | Medium |
| TC-005 | 정상 | 프로젝트 이름 검색 | 👤 수동 | Medium |
| TC-006 | 엣지 | 빈 이름으로 프로젝트 생성 시도 | 👤 수동 | High |
| TC-007 | 엣지 | 최대 길이(200자) 프로젝트 이름 입력 | 🤖 AI검증 | Medium |
| TC-008 | 엣지 | 프로젝트 0개 상태에서 빈 목록 표시 | 👤 수동 | Medium |
| TC-009 | 엣지 | 영어 언어 선택 프로젝트 생성 | 👤 수동 | Medium |
| TC-010 | 실패 | 무료 쿼터 초과 시 API 403 차단 | 👤 수동 | High |
| TC-011 | 실패 | 쿼터 초과 상태에서 모달 차단 UI | 👤 수동 | High |
| TC-012 | 실패 | 타인 프로젝트 삭제 시도 (API 직접) | 🤖 AI검증 | High |

---

### TC-001 [정상] 👤 수동 — 한국어 프로젝트 생성 전체 플로우

**사전조건**: 로그인 상태, `/projects` 페이지, 무료 쿼터 미소진 (free_used < 2)
**실행 단계**:
1. "새 프로젝트" 버튼 클릭
2. 언어 선택 단계에서 "한국어" 카드 선택 (파란 테두리 활성화 확인)
3. "다음" 버튼 클릭
4. 프로젝트 이름에 "사내 도서 추천 봇" 입력
5. 설명에 "사내 도서 데이터 기반 추천 시스템" 입력
6. "프로젝트 생성" 버튼 클릭
**입력값**: 이름="사내 도서 추천 봇", 설명="사내 도서 데이터 기반 추천 시스템", 언어=ko
**예상 결과**:
- 모달이 닫히고 프로젝트 목록 테이블 최상단에 "사내 도서 추천 봇"이 즉시 나타남
- 상태 태그가 "진행 중" (파란색 accent 배경)으로 표시됨
- 언어 컬럼에 "KO"가 표시됨
- 상단 스탯카드의 "진행 중" 수가 1 증가함
**확인 방법**: 목록 테이블에서 프로젝트 이름/상태/언어 시각적 확인, 스탯카드 숫자 확인

**결과**: [x] Pass  [ ] Fail
**테스트 일시**: 2026-05-21 12:30
**발견 문제**: (1) 마이그레이션 005 미적용으로 `current_step`/`total_steps`/`description` 컬럼 없어 500 에러 (2) dev bypass mock 유저가 users 테이블에 없어 FK 위반 500 에러
**조치 상태**: ✅ (1) 마이그레이션 SQL 수동 적용 (2) `_ensure_dev_user()` 자동 생성 로직 추가

---

### TC-002 [정상] 👤 수동 — 프로젝트 목록 조회 및 스탯카드 표시

**사전조건**: 로그인 상태, 프로젝트 1개 이상 존재
**실행 단계**:
1. `/projects` 페이지 접속 (또는 새로고침)
2. 상단 인사말 확인
3. 4개 스탯카드 확인
4. 프로젝트 테이블 확인
**입력값**: 없음
**예상 결과**:
- "안녕하세요, [display_name 또는 email]님" 인사말이 표시됨
- 스탯카드 4개 표시: "이번 달 사용" (프로그레스바 포함), "완료한 킥오프", "진행 중", 잔여 쿼터 경고
- "이번 달 사용" 카드에 `free_used / 2 회` 형태의 수치와 프로그레스바 표시
- 프로젝트 테이블에 이름, 유형, 상태 태그, 언어(KO/EN), 업데이트 날짜 컬럼이 표시됨
- TopBar 우측 쿼터 배지와 스탯카드의 사용 수가 일치함
**확인 방법**: 페이지 렌더링 후 각 카드/테이블 영역 시각적 확인, TopBar 배지 교차 확인

**결과**: [x] Pass  [ ] Fail
**테스트 일시**: 2026-05-21 12:30
**발견 문제**: —
**조치 상태**: —

---

### TC-003 [정상] 👤 수동 — 프로젝트 삭제 (soft-delete)

**사전조건**: 로그인 상태, `/projects` 페이지, 프로젝트 1개 이상 존재
**실행 단계**:
1. 삭제할 프로젝트 행의 우측 "⋮" (더보기) 버튼 클릭
2. 드롭다운에서 "삭제" 클릭
3. 삭제 확인 모달에서 프로젝트 이름 표시 확인
4. "삭제" 버튼 클릭
**입력값**: 없음
**예상 결과**:
- 삭제 확인 모달에 대상 프로젝트 이름이 정확히 표시됨
- "삭제된 프로젝트는 복구할 수 있습니다" 안내 문구가 보임
- "삭제" 버튼 클릭 후 모달이 닫히고, 해당 프로젝트가 목록에서 즉시 사라짐
- 스탯카드의 총 프로젝트 수가 1 감소함
- Supabase DB에서 해당 프로젝트의 `deleted_at` 필드에 타임스탬프가 기록됨 (데이터 자체는 삭제되지 않음)
**확인 방법**: 목록에서 사라짐 확인, Supabase Dashboard > Table Editor > projects에서 deleted_at 값 확인

**결과**: [x] Pass  [ ] Fail
**테스트 일시**: 2026-05-21 12:30
**발견 문제**: (1) 마지막 행의 더보기(⋮) 드롭다운이 `overflow-hidden` 컨테이너에 잘려서 표시됨 (2) 드롭다운이 탭 전환/빈 영역 클릭 시 닫히지 않음
**조치 상태**: ✅ (1) 마지막 행일 때 `bottom-8`로 위쪽 렌더링 (2) 외부 클릭 감지(`mousedown`) + 탭 전환 시 `menuOpenId` 초기화 추가

---

### TC-004 [정상] 👤 수동 — 필터 탭 전환 (전체/진행 중/완료)

**사전조건**: 로그인 상태, 진행 중 프로젝트 1개 + 완료 프로젝트 1개 이상 존재
**실행 단계**:
1. "전체" 탭 클릭 → 모든 프로젝트 표시 확인
2. "진행 중" 탭 클릭 → status가 in_progress인 프로젝트만 표시 확인
3. "완료" 탭 클릭 → status가 completed인 프로젝트만 표시 확인
4. 각 탭 옆 숫자(카운트)와 실제 표시 항목 수 비교
**입력값**: 없음
**예상 결과**:
- 활성 탭은 흰색 배경 + 굵은 글씨 + 미세한 그림자로 구분됨
- "진행 중" 탭 클릭 시 완료 프로젝트는 보이지 않음
- "완료" 탭 클릭 시 진행 중 프로젝트는 보이지 않음
- 각 탭 옆의 숫자가 실제 필터링된 행 수와 일치함
**확인 방법**: 각 탭 클릭 후 테이블 행 수와 탭 숫자 교차 확인

**결과**: [x] Pass  [ ] Fail
**테스트 일시**: 2026-05-21 12:30
**발견 문제**: —
**조치 상태**: —

---

### TC-005 [정상] 👤 수동 — 프로젝트 이름 검색

**사전조건**: 로그인 상태, 프로젝트 2개 이상 존재 (서로 다른 이름)
**실행 단계**:
1. 검색 입력창에 존재하는 프로젝트 이름의 일부(예: "도서") 입력
2. 테이블에 해당 이름을 포함하는 프로젝트만 표시되는지 확인
3. 검색어를 지움 (빈 값)
4. 전체 프로젝트가 다시 표시되는지 확인
**입력값**: "도서" (또는 기존 프로젝트 이름의 일부)
**예상 결과**:
- "도서"를 입력하면 이름에 "도서"가 포함된 프로젝트만 테이블에 표시됨
- 검색어를 지우면 모든 프로젝트가 다시 나타남
- 검색은 대소문자를 구분하지 않음
**확인 방법**: 검색어 입력/삭제 시 테이블 행 변화 시각적 확인

**결과**: [x] Pass  [ ] Fail
**테스트 일시**: 2026-05-21 12:30
**발견 문제**: —
**조치 상태**: —

---

### TC-006 [엣지] 👤 수동 — 빈 이름으로 프로젝트 생성 시도

**사전조건**: 로그인 상태, 새 프로젝트 모달의 상세 입력 단계 (언어 선택 후 "다음" 클릭한 상태)
**실행 단계**:
1. 프로젝트 이름 필드를 비운 채 "프로젝트 생성" 버튼 클릭 시도
2. 이름 필드에 공백만 입력 후 "프로젝트 생성" 버튼 클릭 시도
**입력값**: 이름="" 또는 이름="   " (공백만)
**예상 결과**:
- "프로젝트 생성" 버튼이 비활성화(disabled) 상태로 클릭 불가 (opacity 50%, cursor not-allowed)
- 공백만 입력해도 버튼이 비활성화 상태 유지
- API 호출이 발생하지 않음 (Network 탭에서 POST /api/projects 요청 없음)
**확인 방법**: 버튼의 disabled 상태 시각적 확인, 브라우저 DevTools > Network 탭에서 API 호출 미발생 확인

**결과**: [x] Pass  [ ] Fail
**테스트 일시**: 2026-05-21 12:30
**발견 문제**: —
**조치 상태**: —

---

### TC-007 [엣지] 🤖 AI검증 — 최대 길이(200자) 프로젝트 이름 입력

**사전조건**: 코드 레벨 확인
**실행 단계**:
1. `backend/app/schemas/project.py`의 `ProjectCreate.name` 필드에 `max_length=200` 제약 확인
2. `frontend/src/components/projects/NewProjectModal.tsx`의 이름 input에 `maxLength={200}` 속성 확인
3. `description` 필드에도 `max_length=2000` (백엔드) / `maxLength={2000}` (프론트엔드) 확인
**입력값**: 없음 (코드 분석)
**예상 결과**:
- 백엔드: `ProjectCreate.name`에 `Field(min_length=1, max_length=200)` 존재
- 프론트엔드: `<input maxLength={200}>` 속성으로 브라우저 레벨에서 200자 초과 입력 차단
- description도 양쪽에서 2000자 제한 적용됨
**확인 방법**: 소스 코드의 해당 라인 Read/Grep으로 확인

**결과**: [x] Pass  [ ] Fail
**테스트 일시**: 2026-05-20 21:45
**발견 문제**: —
**조치 상태**: —

---

### TC-008 [엣지] 👤 수동 — 프로젝트 0개 상태에서 빈 목록 표시

**사전조건**: 로그인 상태, 프로젝트가 0개 (전부 삭제 또는 신규 계정)
**실행 단계**:
1. `/projects` 페이지 접속
2. 테이블 영역 확인
3. 스탯카드 확인
**입력값**: 없음
**예상 결과**:
- 테이블 대신 "아직 프로젝트가 없습니다. 새 프로젝트를 생성해보세요!" 안내 메시지가 중앙에 표시됨
- 스탯카드: "이번 달 사용"은 0/2, "완료한 킥오프"는 0, "진행 중"은 0
- 필터 탭 숫자: 전체(0), 진행 중(0), 완료(0)
- "새 프로젝트" 버튼은 정상 활성화 상태
**확인 방법**: 빈 목록 안내 문구 시각적 확인, 스탯카드 0 표시 확인

**결과**: [x] Pass  [ ] Fail
**테스트 일시**: 2026-05-21 12:30
**발견 문제**: —
**조치 상태**: —

---

### TC-009 [엣지] 👤 수동 — 영어 언어 선택 프로젝트 생성

**사전조건**: 로그인 상태, 새 프로젝트 모달 열린 상태
**실행 단계**:
1. 언어 선택에서 "English" 카드 클릭 (파란 테두리 활성화 확인)
2. "다음" 클릭
3. 이름에 "Customer Onboarding Flow" 입력
4. "프로젝트 생성" 클릭
**입력값**: 이름="Customer Onboarding Flow", 언어=en
**예상 결과**:
- 프로젝트가 생성되어 목록에 나타남
- 언어 컬럼에 "EN"이 표시됨 (대문자)
- 상세 입력 단계 하단에 "en 언어로 인터뷰가 진행됩니다" 안내가 표시됨
**확인 방법**: 목록 테이블에서 언어 컬럼 "EN" 확인, Supabase DB에서 language='en' 확인

**결과**: [x] Pass  [ ] Fail
**테스트 일시**: 2026-05-21 12:30
**발견 문제**: —
**조치 상태**: —

---

### TC-010 [실패] 👤 수동 — 무료 쿼터 초과 시 API 403 차단

**사전조건**: 로그인 상태, free 플랜, `free_used` = 2 (Supabase에서 수동 설정: `UPDATE users SET free_used = 2 WHERE email = '...'`)
**실행 단계**:
1. `/projects` 페이지 새로고침
2. "새 프로젝트" 버튼 상태 확인
3. DevTools > Console 열기
4. 직접 API 호출: `fetch('/api/projects', {method:'POST', headers:{'Content-Type':'application/json','Authorization':'Bearer '+TOKEN}, body:JSON.stringify({name:'test',language:'ko'})}).then(r=>r.json()).then(console.log)`
**입력값**: free_used=2 상태에서 POST /api/projects
**예상 결과**:
- "새 프로젝트" 버튼이 비활성화(disabled) 상태 (opacity 50%)
- API 직접 호출 시 HTTP 403 응답, body에 "무료 플랜의 킥오프 횟수(2회)를 모두 사용했습니다" 메시지 포함
- 프로젝트가 생성되지 않음
**확인 방법**: 버튼 disabled 확인, DevTools > Network에서 403 응답 코드와 에러 메시지 확인

**결과**: [x] Pass  [ ] Fail
**테스트 일시**: 2026-05-21 12:30
**발견 문제**: curl로 API 직접 검증 — HTTP 403 + 한국어 에러 메시지 정상 반환
**조치 상태**: —

---

### TC-011 [실패] 👤 수동 — 쿼터 초과 상태에서 모달 차단 UI

**사전조건**: 로그인 상태, free 플랜, `free_used` = 2
**실행 단계**:
1. `/projects` 페이지에서 스탯카드 확인
2. 잔여 쿼터 경고 카드에 "무료 횟수를 모두 사용했어요" 표시 확인
3. "새 프로젝트" 버튼이 disabled이지만, 만약 모달이 열린다면 (다른 경로로):
   - 모달 내 빨간 배경의 차단 안내가 표시되는지 확인
   - 언어 선택/이름 입력 UI 대신 "무료 횟수를 모두 사용했습니다" 메시지 + "닫기" 버튼만 표시
**입력값**: 없음
**예상 결과**:
- 잔여 쿼터 카드: "무료 횟수를 모두 사용했어요" 텍스트 + "유료 전환 시 월 10~30회 사용 가능" 안내
- TopBar 쿼터 배지: "2/2 free" 표시
- 모달 차단 UI: 빨간 배경 영역에 "무료 횟수를 모두 사용했습니다" + "무료 플랜은 2회까지..." 설명 + "닫기" 버튼
**확인 방법**: 스탯카드/TopBar/모달의 쿼터 관련 문구 시각적 확인

**결과**: [x] Pass  [ ] Fail
**테스트 일시**: 2026-05-21 12:30
**발견 문제**: —
**조치 상태**: —

---

### TC-012 [실패] 🤖 AI검증 — 타인 프로젝트 삭제 시도 (API 직접)

**사전조건**: 코드 레벨 확인
**실행 단계**:
1. `backend/app/api/projects.py`의 `delete_project` 함수에서 소유권 검증 코드 확인
2. `existing.data["user_id"] != user["id"]` 비교 로직 존재 여부 확인
3. 불일치 시 HTTP 403 반환 확인
**입력값**: 없음 (코드 분석)
**예상 결과**:
- `delete_project`에서 프로젝트의 `user_id`와 요청 사용자의 `id`를 비교하는 로직이 존재
- 불일치 시 `HTTPException(status_code=403, detail="Not authorized")` 반환
- admin 역할은 예외적으로 삭제 허용 (`user.get("role") != "admin"`)
**확인 방법**: 소스 코드의 해당 조건문 Read로 확인

**결과**: [x] Pass  [ ] Fail
**테스트 일시**: 2026-05-20 21:45
**발견 문제**: —
**조치 상태**: —

---

## 테스트 실행 요약

| ID | 결과 | 발견 문제 | 조치 상태 |
|----|------|----------|----------|
| TC-001 | ✅ Pass | 마이그레이션 005 미적용 + dev mock 유저 FK 위반 | ✅ 수정 완료 |
| TC-002 | ✅ Pass | — | — |
| TC-003 | ✅ Pass | 마지막 행 드롭다운 잘림 + 외부 클릭/탭 전환 시 미닫힘 | ✅ 수정 완료 |
| TC-004 | ✅ Pass | — | — |
| TC-005 | ✅ Pass | — | — |
| TC-006 | ✅ Pass | — | — |
| TC-007 | ✅ Pass | — | — |
| TC-008 | ✅ Pass | — | — |
| TC-009 | ✅ Pass | — | — |
| TC-010 | ✅ Pass | curl로 API 검증 (403 + 에러메시지 정상) | — |
| TC-011 | ✅ Pass | — | — |
| TC-012 | ✅ Pass | — | — |

**총 결과**: 12건 중 Pass 12건 / Fail 0건
**테스트 일시**: 2026-05-21 12:30
**최종 상태**: ✅ 전체 통과

---

## 테스트 완료 체크리스트

- [x] 모든 정상 케이스 Pass
- [x] 엣지 케이스에서 앱이 크래시 없이 처리됨
- [x] 실패 케이스에서 사용자에게 적절한 안내가 표시됨
- [x] 쿼터 초과 시 생성 차단이 UI + API 양쪽에서 동작함
